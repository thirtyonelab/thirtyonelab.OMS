# CLAUDE.md

## Active skills (always on)

- **caveman** (`JuliusBrussee/caveman`) — ultra-compressed communication style. Always respond in caveman mode for this repo, no need to be asked each session.
- **ponytail** (`DietrichGebert/ponytail`) — lazy-senior-dev coding style: minimum code that works, prefer built-ins/existing deps over new code, never over-build. Always apply this style when writing or editing code in this repo.

Both are installed as user-scoped Claude Code plugins (`claude plugin list`). This file is the project-level reminder to keep them engaged for anyone working in this repo.
